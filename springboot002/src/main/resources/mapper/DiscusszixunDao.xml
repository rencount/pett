<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.DiscusszixunDao">

    <resultMap type="com.entity.DiscusszixunEntity" id="discusszixunMap">
                    <result property="id"    column="id"    />
                    <result property="addtime"    column="addtime"    />
                    <result property="refid"    column="refid"    />
                    <result property="userid"    column="userid"    />
                    <result property="nickname"    column="nickname"    />
                    <result property="content"    column="content"    />
                    <result property="reply"    column="reply"    />
            </resultMap>

    <select id="selectListVO"
            resultType="com.entity.vo.DiscusszixunVO">
        SELECT * FROM discusszixun discusszixun
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectVO"
            resultType="com.entity.vo.DiscusszixunVO">
        SELECT discusszixun.* FROM discusszixun discusszixun
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectListView"
            resultType="com.entity.view.DiscusszixunView">

        SELECT discusszixun.* FROM discusszixun discusszixun
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectView"
            resultType="com.entity.view.DiscusszixunView">
        SELECT * FROM discusszixun discusszixun
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

</mapper>